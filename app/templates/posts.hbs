<h1>posts</h1>
<div class="item-list">
	
	<ul class="list-unstyled">
		{{#each model}}
		<li class="media post" data-id="{{id}}">
			{{#link-to 'posts' this}} <img class="media-object img-thumbnail" alt="{{title}}" {{bind-attr src=image}}/>
			{{/link-to}}
			<div class="media-body">
				<h4 class="media-heading">{{title}}</h4>
				<p>
					{{body}}
				</p>
			</div>
		</li>
		{{/each}}
	</ul>
</div>
<div class="item-detail"></div>
 